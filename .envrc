# direnv — juris (uv). Ativa a venv ao entrar na pasta e carrega .env se existir.
export VIRTUAL_ENV="$PWD/.venv"
[ -d "$VIRTUAL_ENV" ] && PATH_add "$VIRTUAL_ENV/bin"
watch_file uv.lock pyproject.toml
dotenv_if_exists .env
